{
  "name": "maou-gunryaku",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build && vite build --config vite.chara.config.ts && node scripts/chara-pages.mjs",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "~5.9",
    "vite": "^8.3.1",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
